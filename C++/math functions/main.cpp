#include <iostream>

#include <cmath>

int main() {

    double x = 3;
    double y = 4;
    double z;

    // z = std::max(x, y); // maximo valor entre os dois valores
    // z = std::min(x, y);

    // --------------- cmath --------------------
    // z = pow(2, 3); // seria 2^3 
    // z = sqrt(9); raiz quadrada
    // z = abs(-3); transforma o valor em positivo
    std::cout << z;



    return 0;
}